import React from 'react';

export default function Profile(){
    return(
        <div>
            <h2> Your profile</h2>
            {/* I should include the user name, email and a display of the list of events */}
        </div>
    );
};