import React from "react";
export default function Settings(){
    return(
        <div>
            <h2>Settings</h2>
        </div>
        // theme settings and the option of deleting account
    );
};